<h1>The Employees of Dunder Mifflin</h1>
<ul>
<% @employees.each do |e| %>
    <li><%= link_to "#{e.first_name} #{e.last_name}", employee_path(e) %></li>
    <% end %>
    </ul>
<br>
<hr>
<%= button_to "Create Employee", new_employee_path(@employee), method: :get %>