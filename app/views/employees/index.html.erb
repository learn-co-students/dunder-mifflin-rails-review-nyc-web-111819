<h1> All Employees</h1>

<%= link_to "New Employee", new_employee_path %>
<ul>
<% @employees.each do |e| %>
  <li><%= link_to e.first_name + " " + e.last_name, employee_path(e) %></li>
<% end %>
</ul>
